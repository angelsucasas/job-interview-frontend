export const employee_URL = Object.freeze({    
    EMPLOYEE: "employee",
    EMPLOYEE_ACCOUNT: "employee/account"
});

export const TRANSACTION_URL = Object.freeze({
    TRANSACTION: "transaction",
});

export const CURRENCY = Object.freeze({
    USD: {
        id: 1,
        iso: 'USD',
    },
    EUR: {
        id: 2,
        iso: 'EUR',
    },
});
export const ROL = Object.freeze({
    customer: 1,
    ADMIN: 2,
});

export const SUCESS = 'success';
export const FETCHING = 'fetching';
export const FETCHED = 'fetched';
